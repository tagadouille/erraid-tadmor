#include <stdint.h>
/**
 * @brief For testing the reading for an tree structure
 * @param task_dir the directory where the tasks are
 * @param id the id of the task
 */
void test_all(char* task_dir, uint64_t id);

/**
 * @brief for testing the listing of all the task
 * @param task_dir the directory where the tasks are
 */
void test_task_read(char* task_dir);