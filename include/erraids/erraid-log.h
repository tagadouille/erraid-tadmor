/**
 * @brief Write the log message at the log file
 * @param fmt a string with format, it'll be formated an replace by
 * the other parameters of the function
 */
int write_log_msg(const char *fmt, ...);